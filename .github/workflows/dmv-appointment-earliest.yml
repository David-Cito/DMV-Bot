name: DMV Appointment Bot (Earliest Only)

on:
  workflow_dispatch:
    inputs:
      test_notify:
        description: "Send a test notification even if no within-7-day match"
        required: false
        default: "false"
      notify_window_days:
        description: "Notify window in days (within X days)"
        required: false
        default: "14"

jobs:
  run-bot:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.57.0-jammy
    permissions:
      contents: read
    concurrency:
      group: dmv-appointment-bot-earliest
      cancel-in-progress: false
    env:
      DMV_EARLIEST_ONLY: "true"
      DMV_NOTIFY_TEST: ${{ inputs.test_notify || 'false' }}
      DMV_NOTIFY_WINDOW_DAYS: ${{ inputs.notify_window_days || vars.DMV_NOTIFY_WINDOW_DAYS || '14' }}

    steps:
      - name: Random jitter (night hours HST)
        run: |
          hour=$(TZ=Pacific/Honolulu date +%H)
          if [ "$hour" -ge 0 ] && [ "$hour" -lt 6 ]; then
            delay=$((RANDOM % 45))
            echo "Night hours (HST). Sleeping ${delay}s for jitter."
            sleep "$delay"
          else
            echo "Daytime hours (HST). No jitter."
          fi

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: npm ci

      - name: Run DMV appointment bot (earliest only)
        run: npm test

      - name: Upload results to Supabase
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/upload-supabase.js

      - name: Notify users (within 7 days)
        if: always()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          DMV_EMAIL_FROM: ${{ secrets.DMV_EMAIL_FROM }}
        run: node scripts/notifications/send-user-notifications.js
